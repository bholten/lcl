# lcl-posix

POSIX filesystem and path bindings for Lcl.

Build with `-DLCL_BUILD_POSIX=ON`.

POSIX only (Linux, macOS, BSD) by design; not portable to Windows.
Portable ANSI C file I/O lives in `lcl-io`.

Documentation: [docs/Posix.lcl](docs/Posix.lcl) — rendered on the docs site.

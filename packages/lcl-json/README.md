# lcl-json

JSON encoding and decoding for Lcl, backed by cJSON.

Build with `-DLCL_BUILD_JSON=ON`. Built and used in-tree only for
0.1.0 (cJSON is vendored via `FetchContent`; not part of the
`cmake --install` artifact).

Documentation: [docs/Json.lcl](docs/Json.lcl) — rendered on the docs site.

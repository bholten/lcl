# lcl-crypto

Hashing, HMAC, digital signatures, base64 and CSPRNG bytes for Lcl via OpenSSL.

Build with `-DLCL_BUILD_CRYPTO=ON` (needs OpenSSL 1.1+). Portable wherever OpenSSL builds.

Documentation: [docs/Crypto.lcl](docs/Crypto.lcl) — rendered on the docs site.

# lcl-regex

POSIX extended regular expressions for Lcl.

Build with `-DLCL_BUILD_REGEX=ON`. POSIX-only (`regex.h`): Linux,
macOS, BSD; not portable to Windows without a `regex.h` shim.

Documentation: [docs/Regex.lcl](docs/Regex.lcl) — rendered on the docs site.

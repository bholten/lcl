# lcl-math

Floating-point math functions for Lcl, a thin wrapper around the C `<math.h>` library.

Build with `-DLCL_BUILD_MATH=ON`. Portable anywhere a hosted C
implementation with libm is available.

Documentation: [docs/Math.lcl](docs/Math.lcl) — rendered on the docs site.

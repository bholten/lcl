# lcl-io

File I/O for Lcl, bound to the ANSI C standard library and nothing else.

Build with `-DLCL_BUILD_IO=ON`.

Portable everywhere core Lcl builds, including Windows. POSIX-only
filesystem operations live in `lcl-posix`.

Documentation: [docs/Io.lcl](docs/Io.lcl) — rendered on the docs site.
